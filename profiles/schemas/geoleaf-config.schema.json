{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "geoleaf/geoleaf-config",
    "title": "GeoLeaf Root Config",
    "description": "geoleaf.config.json — root configuration file loaded at application startup. Hardened in Sprint S1 (Profile Contract v1, PRF-SCHEMA). modules.<id> blocks stay open (plugin-owned, INV-CONFIG).",
    "type": "object",
    "additionalProperties": false,
    "patternProperties": { "^_comment": {} },
    "properties": {
        "$schema": { "type": "string" },
        "debug": {
            "type": "boolean",
            "default": false,
            "description": "Enable verbose debug logging, a CLOCK-based cache-bust (?t=<epoch ms>) on config requests, and skipBundle (the pre-built profile bundle is ignored, so section files edited in a deployed profile are picked up without a rebuild). \u26a0\ufe0f The clock TAKES PRECEDENCE over `data.profileVersion`: it is a debugging mode, and it defeats every cache on every load \u2014 including the browser cache and the service worker's. It is not the answer to a stale profile in production; `data.profileVersion` is."
        },
        "data": {
            "type": "object",
            "description": "Profile and data configuration.",
            "additionalProperties": false,
            "patternProperties": { "^_comment": {} },
            "properties": {
                "activeProfile": {
                    "type": "string",
                    "description": "Name of the active profile to load (must match a directory in profiles/)."
                },
                "availableProfiles": {
                    "type": "array",
                    "description": "INJECTÉE PAR LE BUILD, jamais écrite à la main : inventaire des profils réellement embarqués dans la variante, récolté au moment de la copie et lu par le sélecteur de profil. ⚠️ Elle n'a de sens que dans un artefact déployé — la source ne sait pas ce qu'une variante embarquera. Déclarée ici parce que le bloc est `additionalProperties: false` et que la configuration déployée doit valider.",
                    "items": {
                        "type": "object",
                        "$comment": "La forme est celle que le build ÉCRIT et que la capacité de sélection LIT — pas une forme plausible. Elle est volontairement fermée : si la récolte gagne un champ, le schéma rougit, ce qui est le couplage recherché. Une forme ouverte aurait laissé la clé injectée dériver du type qui la décrit, ce qui est le défaut d'origine à une variable près.",
                        "additionalProperties": false,
                        "required": ["id", "displayLabel"],
                        "properties": {
                            "id": {
                                "type": "string",
                                "description": "Identifiant du profil, égal au nom de son répertoire sous `profiles/`."
                            },
                            "displayLabel": {
                                "type": "string",
                                "description": "Libellé court du sélecteur. Le build le dérive de `displayLabel`, à défaut `label`, à défaut l'identifiant — il est donc toujours présent."
                            },
                            "icon": {
                                "type": "string",
                                "description": "Emoji facultatif rendu devant le libellé. Omis, et non vide, quand le profil n'en déclare pas."
                            }
                        }
                    }
                },
                "profilesBasePath": {
                    "type": "string",
                    "default": "profiles",
                    "description": "Base path to the profiles directory."
                },
                "profileVersion": {
                    "type": ["string", "number"],
                    "default": 0,
                    "description": "Empreinte de CONTENU du profil, ajoutée en `?t=` à chaque requête de ressource de profil — `profile.json`, `mapping.json`, les fichiers de section, les configs de couche et le bundle. ⚠️ Opaque, et JAMAIS une date : la valeur n'est ni lue ni comparée ni ordonnée, seul importe qu'elle change quand le contenu change — une empreinte, un compteur de révision, un identifiant de déploiement. Une horloge défait le mécanisme, puisqu'elle change à chaque chargement et fait re-télécharger ce qui n'a pas bougé. Fournie par l'APPLICATION INTÉGRANTE, seule à savoir quand son profil a changé ; l'écrire dans `profile.json` est trop tard, le jeton est résolu avant que ce fichier soit requêté. Absente, le jeton vaut `0` — URL fixe, comportement historique conservé, mais un serveur envoyant un `max-age` long épingle alors le profil et toutes ses annexes pour la durée de cet en-tête.",
                    "$comment": "Déclarée ici parce que le bloc est `additionalProperties: false`. Le défaut `0` est le comportement d'avant l'introduction de la clé : la rétro-compatibilité est portée par le défaut, pas par une branche. ⚠️ Dans un DÉPLOYÉ elle est ÉCRITE PAR LE BUILD — `build-deploy.cjs` y pose l'empreinte de contenu de l'arbre `profiles/` (`lib/profile-fingerprint.cjs`) — donc absente de la source et présente dans l'artefact, comme `availableProfiles`. 🛑 Le fichier qui la porte doit être servi `no-cache` : on ne peut pas invalider par un jeton le fichier qui porte ce jeton (gate SC-05, recettes serveur émises avec le livrable)."
                },
                "profileBundle": {
                    "type": "object",
                    "description": "The active profile handed over in memory instead of being fetched. Carries both on-disk artefacts: `profile` (profile.json) and `bundle` (profile-bundle.json). Present, no HTTP request is issued for the profile configuration; absent, the cascade stays the default path."
                },
                "enableProfilePoiMapping": {
                    "type": "boolean",
                    "default": false,
                    "description": "Enable POI data normalization via mapping.json."
                }
            }
        },
        "logging": {
            "type": "object",
            "description": "Logging verbosity.",
            "additionalProperties": false,
            "patternProperties": { "^_comment": {} },
            "properties": {
                "level": {
                    "type": "string",
                    "enum": ["debug", "info", "warn", "error", "production"],
                    "default": "info",
                    "description": "Minimum log level displayed in console."
                }
            }
        },
        "modules": {
            "type": "object",
            "description": "Per-plugin configuration blocks, keyed by module id (Plugin Contract v1, INV-CONFIG). Usually declared in the profile — see profile.schema.json for the known module ids.",
            "additionalProperties": { "type": "object" }
        }
    }
}
