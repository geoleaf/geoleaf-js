---
title: "PWA — see /pwa"
---

# PWA — this page has moved

::: info

**The PWA page is [`../pwa.md`](../pwa.md).** There is only one.

:::

`docs/pwa.md` covers the whole subject: deployment prerequisites, icon generation, branding,
install prompt and per-platform behaviour, programmatic use, Lighthouse checklist, file layout,
and the reference for the `PWAConfig` interface — which TypeDoc resolves through `{@link}`.

This file is kept as a redirect so that existing links and sidebar entries do not break.
