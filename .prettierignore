# Ignorer ces fichiers pour Prettier
node_modules/
dist/
coverage/
artifacts/
*.min.js
*.min.css
package-lock.json

# ARCHI S11 — fichier GÉNÉRÉ par `npm run docs:tree`, vérifié à l'octet près par
# `docs:tree --check` dans ci:local. Le laisser passer sous prettier le fait diverger
# de la sortie du générateur (italiques `*x*` → `_x_`, colonnes de tableau réalignées) :
# la gate devient alors rouge de façon permanente, sans qu'aucune source ait bougé.
# Un artefact généré n'a pas à être formaté — il a à être reproductible.
docs/reference/ARBORESCENCE_QUALIFIEE.md
docs/reference/ARBORESCENCE_QUALIFIEE.html
# Doc V3 Étape 3 item 7 — même régime, même motif : GÉNÉRÉ par
# `npm run gen:profile-schema` depuis `profiles/schemas/*.json`, vérifié à l'octet près par
# `gen:profile-schema:check`. Prettier réaligne les colonnes de tableau, ce qui suffirait à
# rendre la gate rouge en permanence sans qu'un schéma ait bougé.
docs/reference/PROFILE_SCHEMA_REFERENCE.md
# Sprint 2 de `roadmap_collecte-terrain-offline` — même régime, même motif : GÉNÉRÉ par
# `npm run gen:attributes-report` depuis le bloc `attributes` des configs de couche, vérifié
# à l'octet près par `gen:attributes-report:check`. ⚠️ L'oubli n'était pas théorique : la
# gate est sortie ROUGE au premier `ci:local` post-commit, uniquement parce que Prettier
# avait réaligné ses colonnes de tableau — aucune source n'avait bougé.
docs/reference/MODELE_ATTRIBUTAIRE.md
